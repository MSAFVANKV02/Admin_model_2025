import { ICategory } from "@/types/categorytypes"


type Props = {
    product:ICategory
}

export default function CategoryAddModal({
    product,
  
}: Props) {
  return (
    <div>CategoryAddModal</div>
  )
}