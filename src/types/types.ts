export type ICouponTypes = {
    code: string;
    percentage: number;
    minimumAmount: number;
    expiryDate: string;
    usageLimit: number;
    usageCount: number;
    createdAt: string;
    updatedAt: string;
    _id: string;
}